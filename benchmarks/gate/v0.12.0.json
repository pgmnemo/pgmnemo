{
    "version": "v0.12.0",
    "date": "2026-06-24",
    "pgmnemo_version": "0.12.0",
    "gate_status": "PASS",
    "gate_type": "feature_smoke",
    "scope": "v0.12.0 Typed Write API — remember_fact / remember_event / remember_relation with PII-aware state routing, bitemporal supersession, artifact_hash synthesis, and canonical_slug identity (RFC-001 §D2 + ADDENDUM-2 R1–R7).",
    "issues_closed": [],
    "justification": "Correctness gate — not a recall-quality release. The write API ships as feature parity with external memory systems (Mem0, Zep/Graphiti, Letta) that offer typed/structured writes; pgmnemo differentiates by executing the full write+supersession plan inside Postgres (no external graph service, no dual-write round-trip). No '+Xpp recall quality' claim is made or implied. The gate bar is CORRECTNESS of the 7 ADDENDUM-2 requirements: PII routing, artifact_hash synthesis, identity/dedup, ingest_entity compatibility, fail-open embedding, guard_no_test_project, and first-class confidence/pii inputs. All 7 are implemented inside the SQL functions (thin-caller principle).",
    "schema_change": true,
    "schema_change_detail": "New functions: remember_fact(11 params), remember_event(11 params), remember_relation(10 params), canonical_slug(TEXT,TEXT), _has_contact_pii(TEXT), guard_no_test_project(INT,TEXT). New index: ix_entity_canonical_name_prj on agent_lesson for entity hub dedup. No existing column or function signature changed; no breaking schema delta.",
    "scoring_impact": "none on existing recall paths. New rows written via remember_fact/event/relation are content_type-tagged ('fact','event','relation') and participate in existing recall_fast/recall_lessons/recall_hybrid identically to ingest() rows. candidate-state rows (PII) are ghost to default recall (verified_at NULL) — consistent with existing state-machine semantics.",
    "recall_ranking_impact": "none for existing callers. New write functions produce rows indistinguishable from ingest() rows to the recall stack; RRF weights, BM25, vector, and recency signals are unchanged.",
    "new_regress_tests": ["typed_recall_fast", "test_remember_fact"],
    "new_regress_test_count": 2,
    "real_db_integration_tests": {
        "harness": "psycopg2 Python — real PostgreSQL 17.10 (not mocks)",
        "postgres": "17.10",
        "pgvector": "0.8.2",
        "database": "pgmnemo_test (upgraded 0.10.0→0.11.1 via ALTER EXTENSION, then 0.12.0 delta applied directly)",
        "result": "PASS — 33/33 tests",
        "tests_run": 33,
        "tests_passed": 33,
        "date_run": "2026-06-24",
        "bug_found_and_fixed": "AmbiguousColumn: 'WHERE id = _prior.id' inside remember_fact RETURNS TABLE(id BIGINT,...) — PostgreSQL 17 could not distinguish table column from output-set variable. Fixed: qualified to 'WHERE agent_lesson.id = _prior.id' and 'WHERE agent_lesson.id = _prior_id' (3 occurrences in delta + flat). Bug was hidden by best-effort expected output; real-DB test caught it.",
        "coverage": [
            "T1-T3: function signatures (11/11/10 params)",
            "T4-T5: _has_contact_pii PII closed set",
            "T6: canonical_slug normalisation",
            "T7-T8: guard_no_test_project (blocks ≤100, allows 99999)",
            "T9: PII+system → candidate (ADR-61 D4 override)",
            "T10: system+non-PII → validated",
            "T11: agent_authored conf≥0.8+non-PII → validated",
            "T12: agent_authored conf<0.8 → candidate",
            "T13: auto_captured → candidate (even high conf)",
            "T14: artifact_hash synthesized='fact-entity:property', NOT NULL",
            "T15: dedup/merge — same value → same row id, count=1",
            "T16: supersession — new id, prior.state=superseded, total=2",
            "T17: NULL embedding fail-open (id>0)",
            "T18: remember_event id>0, content_type=event, state=validated",
            "T19: remember_relation idempotent, content_type=relation",
            "T20: verified_at=NOT NULL for validated; NULL for PII candidate",
            "T21: explicit has_contact_pii override (R7)"
        ],
        "pg_regress_installcheck": "pending — make installcheck in Docker pg17 (no pg_regress harness in this env); SQL test fixtures in extension/sql/test_remember_fact.sql"
    },
    "real_db_integration_tests_remember_event": {
        "harness": "psycopg2 Python — real PostgreSQL 17.10 (not mocks)",
        "postgres": "17.10",
        "pgvector": "0.8.3",
        "database": "pgmnemo_0120_bench (fresh CREATE DATABASE + pgmnemo 0.11.1 + 0.12.0 delta)",
        "result": "PASS — 28/28 tests",
        "tests_run": 28,
        "tests_passed": 28,
        "date_run": "2026-06-24",
        "coverage": [
            "RE-1: helper reuse (_has_contact_pii, canonical_slug from impl_A)",
            "RE-2: signature (11 params)",
            "RE-3: guard_no_test_project blocks ≤100, allows 99999",
            "RE-4: basic write → positive id",
            "RE-5: content_type=event, topic=entity_key:event:label",
            "RE-6: artifact_hash='event-entity_key:label', NOT NULL (R2)",
            "RE-7a: system → validated",
            "RE-7b: system → verified_at NOT NULL",
            "RE-7c: agent_authored conf≥0.8 → validated",
            "RE-7d: agent_authored conf<0.8 → candidate",
            "RE-7e: auto_captured → candidate",
            "RE-7f: NULL source_type → candidate (default)",
            "RE-8: verified_at gate — validated=NOT NULL, candidate=NULL (ghost)",
            "RE-9: bitemporal t_valid_from=occurred_at; metadata.occurred_at stored",
            "RE-10: append-only — two writes → two distinct ids, count=2",
            "RE-11: NULL embedding fail-open (R5)",
            "RE-12: input validation — invalid entity_key, NULL event_label raise",
            "RE-13: metadata.entity_key + metadata.event_label populated"
        ]
    },
    "significance_required": false,
    "significance_note": "Typed writes are a correctness/parity capability. No recall-quality metric is claimed. Adversarial review (2026-06-24): Memanto (arXiv:2604.22085) achieves 87.1% on LoCoMo vs pgmnemo 84.09% — gap acknowledged, not overclaimed. This release does not change recall mechanics; a benchmarking sprint is tracked separately. The gate bar here is 7/7 ADDENDUM-2 correctness requirements met, not a precision/recall improvement.",
    "addendum_2_checklist": {
        "R1_pii_state_routing": "PASS — PII on person:* → candidate ALWAYS; system non-PII → validated; auto_captured → candidate",
        "R2_artifact_hash_synthesis": "PASS — COALESCE(p_artifact_hash, 'fact-'||entity_key||':'||property) before gate",
        "R3_identity_dedup": "PASS — (lower(topic), project_id) FOR UPDATE; merge same-value; supersede diff-value",
        "R4_ingest_entity_compat": "PASS — version_n=0 prior → new row version_n=1",
        "R5_null_embedding_fail_open": "PASS — NULL embedding accepted and stored; dimension check only on non-NULL",
        "R6_test_guard": "PASS — guard_no_test_project blocks project_id<=100; pg_regress test_remember_fact added",
        "R7_first_class_inputs": "PASS — p_confidence and p_has_contact_pii are function parameters, not caller-derived"
    }
}
